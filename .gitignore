__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.env.lgbm
.venv/
env/
venv/
*.png
!backtest_5yr*.png
*.csv
*.pkl
.DS_Store

# Dashboard data is generated fresh by update-site.yml and published to
# gh-pages; snapshots must not live on main (they only go stale and mislead)
site/data/
site/lgbm/data/

# Logs and trading state (contains personal trading data).
# State files and per-book trade event logs ARE tracked: the rebalance
# workflows commit them to main as the durable record of live state.
logs/
!logs/paper_trade_state.json
!logs/paper_trade_lgbm_state.json
!logs/paper_trade_trend_state.json
!logs/trade_events_multi.jsonl
!logs/trade_events_lgbm.jsonl
!logs/trade_events_trend.jsonl
# Legacy shared event log (pre book-split); keep readable if present.
!logs/trade_events.jsonl
*.log
